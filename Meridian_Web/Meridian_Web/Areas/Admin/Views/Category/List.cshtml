@using Meridian_Web.Areas.Admin.ViewModels.Category
@model List<ListItemViewModel>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title mt-5">Product Categories</h4>
            <a asp-route="admin-category-add" class="btn btn-danger btn-lg mb-5 m">Add new Category</a>
            <div class="table-responsive">
                <table class="table table-dark">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Created</th>
                            <th>Updated</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var category in Model)
                        {
                            <tr>
                                <td>@category.Title</td>
                                <td>@category.CreatedAt</td>
                                <td>@category.UpdatedAt</td>
                                <td class="text-end">
                                    <a asp-route="admin-category-update" asp-route-id="@category.Id" class="btn-outline-secondary p-3">Update</a>

                                    <form class="d-none" method="POST" asp-route="admin-category-delete" asp-route-id="@category.Id" id="delete-@category.Id">
                                    </form>

                                    <button type="submit" form="delete-@category.Id" class="btn-outline-danger p-3">Delete</button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

