@using Meridian_Web.Areas.Admin.ViewModels.Color
@model List<ListItemViewModel>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title mt-5">Product Colors</h4>
            <a asp-route="admin-color-add" class="btn btn-danger btn-lg mb-5 m">Add new Color</a>
            <div class="table-responsive">
                <table class="table table-dark">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Created</th>
                            <th>Updated</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var color in Model)
                        {
                            <tr>
                                <td>@color.Name</td>
                                <td>@color.CreatedAt</td>
                                <td>@color.UpdatedAt</td>
                                <td class="text-end">
                                    <a asp-route="admin-color-update" asp-route-id="@color.Id" class="btn-outline-secondary p-3">Update</a>

                                    <form class="d-none" method="POST" asp-route="admin-color-delete" asp-route-id="@color.Id" id="delete-@color.Id">
                                    </form>

                                    <button type="submit" form="delete-@color.Id" class="btn-outline-danger p-3">Delete</button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

